studentapp-db
sudo dnf install mysql-server -y
sudo systemctl start mysqld
sudo mysql -e "ALTER USER 'root'@'localhost' IDENTIFIED BY 'database@1';"
mysql -u root -p
*****************
give password
*****************

CREATE DATABASE IF NOT EXISTS school_db;
USE school_db;
CREATE TABLE IF NOT EXISTS student_basic(
  id INT AUTO_INCREMENT PRIMARY KEY,
  school_name VARCHAR(100),
  school_branch VARCHAR(100),
  student_name VARCHAR(100),
  student_roll VARCHAR(50)
);

GRANT ALL PRIVILEGES ON school_db.* TO 'root'@'%' IDENTIFIED BY 'database@1';
FLUSH PRIVILEGES;

sudo vim /etc/my.cnf
add bind-address=0.0.0.0 at the bottom
similarly at /etc/my.cnf.d
sudo vim mysql-server.cnf
(check if this path is available or not if present then add bind-address=0.0.0.0 to this file)

as we made the change to cnf file make sure it gets reflected this can be done
if issue still coming
run the below commands in VS terminal 
pip uninstall mysql-connector mysql-connector-python -y
pip install mysql-connector-python
pip show mysql-connector-python

run below commands in studentapp-db:
CREATE USER 'root'@'%' IDENTIFIED BY 'database@1';
GRANT ALL PRIVILEGES ON school_db.* TO 'root'@'%';
FLUSH PRIVILEGES;


Now create studentapp-web ec2 instance
Here we need git and python
so install them
sudo dnf install git
sudo dnf install python

so now both git and python both are installed.
python3 -m pip install mysql-connector-python --> just to update pip (python interpreter to update and compatible to mysql latest version)

clone the python code and run the respective python code
pip3 install mysql-connector-python

=============================
Alter code
=============================
ALTER TABLE student_basic ADD COLUMN student_class VARCHAR(50);
ALTER TABLE student_basic ADD COLUMN maths_unit1_marks INT;
=============================
Python code for adding new entries into TABLE (class and marks)

=============================================
import mysql.connector # for this to run first upgrade pip and install pip install mysql-connector-python

# Ask user for the DB host/IP
db_host = input("Enter the host/IP address of the studentapp-db MySQL server: ")

# Database connection config
db_config = {
    'host': db_host,
    'user': 'root',
    'password': 'database@1',
    'database': 'school_db',
    'port': 3306
}

# Collect basic details from user
school_name = input("Enter School Name: ")
school_branch = input("Enter School Branch: ")
student_name = input("Enter Student Name: ")
student_roll = input("Enter Student Roll Number: ")
student_class = input("Enter Student Class: ")
maths_unit1_marks = int(input("Enter Maths marks for Unit 1: "))

# Insert collected data into DB
conn = mysql.connector.connect(**db_config)
cur = conn.cursor()
cur.execute(
    "INSERT INTO student_basic (school_name, school_branch, student_name, student_roll, student_class, maths_unit1_marks) VALUES (%s, %s, %s, %s, %s, %s)",
    (school_name, school_branch, student_name, student_roll, student_class, maths_unit1_marks)
)
conn.commit()
cur.close()
conn.close()

print("Details saved successfully!")



==============
UPDATE student_basic
SET student_class = '10', maths_unit1_marks = 22
WHERE student_roll = '1';




